# Variables de entorno para Ordéna Frontend

# URL del backend en producción (vacío = usa ruta relativa)
VITE_BACKEND_URL=

# Nota: En producción usamos ruta relativa porque frontend y backend
# están en el mismo dominio. El .htaccess redirige /api/* al backend.
